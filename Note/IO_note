在使用write函数时，最后一个权限参数，必须大于四位，一般从0开始，写作 0644 ，在文件权限管理中 4代表r，2代表w，1代表x，这三个数组通过求和，所得到的的数字，就是该拥有者所拥有的文件操权限
使用write函数时，是进行写入操作，写入操作的参数是 1.进行写入的位置  2.写入的对象 3.写入的字节大小
使用read函数时，是进行读操作，读操作的参数是 1.进行读取的位置 2.将读取的结果写入的对象 3.期待读取的长度
